# 자동 최적화 시스템 추가 의존성
# 기존 requirements.txt에 추가로 필요한 패키지들

# 시스템 모니터링
psutil>=5.9.0           # CPU, 메모리, 프로세스 정보
GPUtil>=1.4.0           # GPU 사용률 모니터링 (선택사항)

# 성능 측정
py3nvml>=0.2.7          # NVIDIA GPU 메모리 정보 (CUDA 환경)

# 설정 관리
pyyaml>=6.0             # YAML 설정 파일 파싱 (이미 있을 수 있음)
jsonschema>=4.0         # 설정 검증 (선택사항)